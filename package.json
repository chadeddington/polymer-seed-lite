{
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.7.7",
    "gulp": "^3.8.5",
    "gulp-load-plugins": "^0.10.0",
    "run-sequence": "^1.0.2"
  },
  "scripts": {
    "test": "gulp test:local",
    "start": "gulp serve"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "polybuild": "^1.1.0"
  }
}
